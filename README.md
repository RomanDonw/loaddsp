# Simple PipeWire-based (my) DSP-filters modules' loader.

## Description
~~idk what say about this project. its just uses PipeWire => works only on Linux.~~

Simple loader of my own DSP-modules (PipeWire filters) with my own format.

## Usage
```
./loaddsp <relative path to DSP module (.so library). for example ./dspmod0.so> [optional arguments passed to DSP filter module]
```